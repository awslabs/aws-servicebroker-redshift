- name: Deprovision Redshift
  hosts: localhost
  gather_facts: false
  connection: local
  roles:
  - role: ansible.kubernetes-modules
    install_python_requirements: no

  - role: deprovision-redshift-apb-openshift
    playbook_debug: false
